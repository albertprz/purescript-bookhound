{
  "private": true,
  "devDependencies": {
    "parcel": "2.7.0",
    "purescript": "^0.15.4",
    "spago": "^0.20.9"
  },
  "scripts": {
    "build": "purs-tidy format-in-place ./src/ && purs-tidy format-in-place ./test/ && spago build",
    "build-ci": "spago -x test.dhall build",
    "test": "spago -x test.dhall test"
  }
}
